# CyberGate Dashboard

Next.js 14 + Tailwind + Framer Motion + Three.js cyberpunk UI.

Run:
```
pnpm install
pnpm dev
```

Env:
```
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_WS_URL=ws://localhost:8080
```
